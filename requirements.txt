# Core
pyembroidery>=1.5.0
openpyxl>=3.1.0
olefile>=0.47

# Web server
fastapi>=0.104.0
uvicorn>=0.24.0
python-multipart>=0.0.6
python-dotenv>=1.0.0

# Desktop app (optional, not needed for web deployment)
pystray>=0.19.0; sys_platform == "win32"
Pillow>=10.0.0; sys_platform == "win32"

# Legacy (optional)
customtkinter>=5.2.0; sys_platform != "linux"
pywinauto>=0.6.8; sys_platform == "win32"
